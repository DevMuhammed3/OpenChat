{
  "name": "@openchat/desktop",
  "version": "1.0.0",
  "main": "build/main.js",
  "scripts": {
    "dev": "pnpm run build && pnpm exec electron .",
    "build": "tsc -p tsconfig.json",
    "prepare:renderer": "node ./scripts/prepare-renderer.mjs",
    "package": "pnpm run build && pnpm exec electron-builder",
    "dist": "pnpm run build && pnpm exec electron-builder"
  },
  "dependencies": {
  },
  "devDependencies": {
    "electron": "^34.5.8",
    "electron-builder": "^25.1.8",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
